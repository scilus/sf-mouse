report_comment: >
  This report has been generated by the <a href="https://github.com/scilus/sf-mouse/" target="_blank">sf-mouse</a>
  analysis pipeline.

report_section_order:
  pre_qc_rgb:
    order: -200
  pre_qc_sampling_mqc:
    order: -300
  registration_qc:
    order: -500
  dti_qc:
    order: -600
  tracking_mask_qc:
    order: -700    
  tracking_qc:
    order: -800

custom_data:
  pre_qc_rgb:
    file_format: "png"
    section_name: "PRE QC - RGB Pre/Post correction"
    description: |
      RGB map from RAW data versus RGB map after gradient correction. Corpus callosum should be in red.
      ### **Using the option use_preqc is highly experimental. Please be careful.**
      <div style="background-color: #ffcccc; color: red; padding: 10px; border: 2px solid red;">
        Be careful: If the colours match the reference image, it is important to ALSO look at the
        eigenvectors of the tensors from the DTI model located in the folder (<code>dwi/sub-01_model-tensor_param-v1_dwimap.nii.gz</code>) 
        to ensure that the orientation is correct (see <a href="https://scilus.github.io/sf-mouse/guides/faq/" target="_blank">FAQ</a>).
      </div>
    plot_type: "image"

  pre_qc_sampling_mqc:
    file_format: "png"
    section_name: "PRE QC - Raw/Optimized Sampling"
    description: |
      Gradient sampling scheme from RAW data (left) and optimized gradient sampling scheme with the same number of directions as RAW data (right).
    plot_type: "image"

  dti_qc:
    file_format: "png"
    section_name: "DTI QC"
    description: |
      This section contains QC images for diffusion tensor imaging (DTI) metric
      maps. Add specifications regarding how to evaluate those images, for example:
      To assess the quality of the DTI metrics, ensure that FA highlights major white
      matter tracts with expected high values (e.g., corpus callosum, corticospinal tract, etc.)...
    plot_type: "image"

  registration_qc:
    file_format: "gif"
    section_name: "Registration QC"
    description: |
      This section contains QC images for the registration of the diffusion data to the
      anatomical space. Add specifications regarding how to evaluate those images, for example:
      To assess the quality of the registration, ensure that the diffusion data is well aligned
      with the anatomical data and that there are no significant misalignments or distortions...
    plot_type: "image"

  tracking_mask_qc:
    file_format: "png"
    section_name: "Tracking Mask QC"
    description: |
      This section contains QC images for the tracking mask of the diffusion data. Add specifications
      regarding how to evaluate those images, for example:
      To assess the quality of the tracking, ensure that the streamlines are well-defined and
      follow expected anatomical pathways without excessive crossing or noise...
    plot_type: "image"

  tracking_qc:
    file_format: "png"
    section_name: "Tracking QC"
    description: |
      This section contains QC images for the tracking of the diffusion data. Add specifications
      regarding how to evaluate those images, for example:
      To assess the quality of the tracking, ensure that the streamlines are well-defined and
      follow expected anatomical pathways without excessive crossing or noise...
    plot_type: "image"

sp:
  pre_qc_rgb:
    fn: "*rgb_mqc.png"
  pre_qc_sampling_mqc:
    fn: "*__sampling_mqc.png"
  registration_qc:
    fn: "*registration_ants_mqc.gif"
  dti_qc:
    fn: "*dti_mqc.png"
  tracking_mask_qc:
    fn: "*_mask_tracking_mqc.png"
  tracking_qc:
    fn: "*local_tracking_mqc.png"